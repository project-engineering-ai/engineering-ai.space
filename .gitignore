# Hugo
/public/
/resources/
.hugo_build.lock

# Тема вендорена в themes/ — исходники остаются в репозитории (см. README)
# и не исключаются: сборка в CI идёт без обращения к сети.

# Python-кэш от scripts/make_icons.py
__pycache__/
*.pyc
